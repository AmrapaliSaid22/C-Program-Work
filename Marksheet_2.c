#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main() {
	
	if((no%3==0)&&(n0%5==0))
	{
		printf("\n%d is div by 3 and 5",no);
		
	}
	else if (no)
	return 0;
}
